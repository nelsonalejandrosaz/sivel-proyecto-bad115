@model IEnumerable<sivel_proyecto_bad115.Models.TIPO_MOVIMIENTOS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TIPO_MOVIMIENTO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TIPO_MOVIMIENTO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_TMOVIMIENTO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_TMOVIMIENTO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_TMOVIMIENTO })
        </td>
    </tr>
}

</table>
